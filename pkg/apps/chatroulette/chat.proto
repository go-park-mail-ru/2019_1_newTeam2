syntax = "proto3";

// protoc --go_out=plugins=grpc:. *.proto

package chatroulette;

message Username {
  string username = 1;
}

message Message {
  string message = 1;
}

message Status {
  bool CheckStatus = 1;
}

service Chatroulette {
    rpc Check (Username) returns (Status) {}
}